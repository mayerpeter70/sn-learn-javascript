//
// L14S02 - Truthy and Falsy
//

// What about numbers
//
var num = 0; // <== try with different numbers
gs.info(num + ' is ' + ((num) ? 'true' : 'false')); // 0 and only 0 is considered to be 'false', any other number (incl. negative ones) returns 'true'
